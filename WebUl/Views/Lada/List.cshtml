@using WebUl.Models
@using WebUl.HtmlHelpers
@model LadaListViewModel
@{
    ViewBag.Title = "Lada";
}
<div>
    <h3>ВИКИНГИ:</h3>
    <table>
        <tr>
            <th>
                Название
            </th>

            <th>
                Модель
            </th>

            <th>
                цена
            </th>

        </tr>
        @foreach (var p in @Model.Lada)
            {
            <tr>
                <td><p>@p.Name </p></td>
                <td><p>@p.Model</p></td>
                <td><p>@p.Price.ToString("# руб")</p></td>
            </tr>
        }
    </table>
</div>
<div>
    @Html.PageLinks(Model.PagingInfo, x => Url.Action("List", new { page = x }))
    <a href="@Url.Action("Index","Home")"><h4>Главная</h4></a>
</div>
